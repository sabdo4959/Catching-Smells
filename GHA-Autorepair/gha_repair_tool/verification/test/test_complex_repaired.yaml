name: Complex Workflow
on:
  push:
    branches: [main]  # develop 브랜치 제거
  pull_request:
    types: [opened, synchronize]
jobs:
  test:
    runs-on: ubuntu-latest
    if: github.event_name == 'push' && contains(github.ref, 'main')
    steps:
      - name: Checkout
        uses: actions/checkout@v2
